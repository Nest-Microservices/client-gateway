export enum OrderStatus {
    CANCELLED = 'CANCELLED',
    DELIVERED = 'DELIVERED',
    PENDING = 'PENDING',
}

export const orderStatusList = [
    OrderStatus.CANCELLED,
    OrderStatus.DELIVERED,
    OrderStatus.PENDING,
]